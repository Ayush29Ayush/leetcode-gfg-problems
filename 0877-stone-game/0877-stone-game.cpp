class Solution {
public:
    //! Solution 1 : Since Alice is starting first, from wherever she starts the winner will always be Alice since her sum will be greater than Bob
    bool stoneGame(vector<int>& piles) {
        return true;
    }
};